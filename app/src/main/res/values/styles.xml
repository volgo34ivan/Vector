<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/color_darkBlue</item>
        <item name="android:popupMenuStyle">@style/PopupMenu.MyStyle</item>
    </style>

    <style name="PopupMenu.MyStyle" parent="android:Widget.PopupMenu">
        <item name="android:popupBackground">@color/color_darkBlue</item>
    </style>


</resources>
